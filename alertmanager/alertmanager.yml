route:
  receiver: "healer-webhook"
  group_by: ["alertname"]
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 2m

receivers:
  - name: "healer-webhook"
    webhook_configs:
      - url: "http://healer:5001/alert"
        send_resolved: false
